package uk.nightcrawler.core.sql

import io.getquill.*
import uk.nightcrawler.core.models.base.BaseEntity

import java.util.UUID

object BaseEntityQuery {
//  extension [A <: BaseEntity](inline value: Query[A]) {
//    inline def findById(inline id: Long): Query[A] = quote {
//      value.filter(_.id == id).take(1)
//    }
//
//    inline def findByUUID(inline uuid: UUID): EntityQuery[A] = ???
//
//
//    // Retrieving
//    inline def find(inline id: Long): Option[A] = ???
//    inline def find(inline uuid: UUID): Option[A] = ???
//  }
}
